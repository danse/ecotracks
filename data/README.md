Code for fetching and transforming the data. It has its own cabal
config. I recommend to initialise a Cabal sandbox in this folder.

    data $ cabal sandbox init
    data $ cabal install
    data $ make
    